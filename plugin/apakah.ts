import { Context } from "telegraf";
import { ContextMessage } from "../lib/constant";

export = {
    name: 'apakah',
    aliases: ['is'],
    description: "Features apakah",
    execute(ctx: Context, message: ContextMessage) {
        
    }
}